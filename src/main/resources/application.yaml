springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/swagger"
    operations-sorter: "method"
#  show-actuator: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 3000
